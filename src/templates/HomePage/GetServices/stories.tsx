import { Story, Meta } from '@storybook/react'
import GetServices from '.'

export default {
  title: 'Modules/GetServices',
  component: GetServices
} as Meta

export const Default: Story = () => <GetServices />
