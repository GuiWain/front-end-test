import styled from 'styled-components'

import UseBase from 'templates/Base'
import UseHero from './Hero'
import UseGetServices from './GetServices'

export const Base = styled(UseBase)``
export const Hero = styled(UseHero)``
export const GetServices = styled(UseGetServices)``
